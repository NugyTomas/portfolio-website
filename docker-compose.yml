services:
  portfolio-website:
    container_name: "portfolio-website"
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
    volumes:
     - ./instance:/app/instance